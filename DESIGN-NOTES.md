# 設計メモ（経緯・判断理由・実測値）

`README.md` が「どう使うか」を書いた取扱説明書なのに対し、この文書は
**「なぜそうなっているか」** を残すためのものです。
コードやコミットメッセージだけからは読み取れない、設計時のやりとり・
測定結果・落とし穴を記録しています。

> この文書は 2026-09-18 時点の記録です。
> 以降の変更で数値や構成が変わっている場合は、コードが正です。
>
> 初版はクラウド側のセッション（初回コミット `4dbf155`）で書かれ、
> 同日の PC 側セッション（`cf48e97`〜`625f211`）の内容を追記しています。
> 「PC側」と書いてある箇所がそれです。

---

## 1. 前提（すり合わせで決まったこと）

実装前のやりとりで確定した要件です。コードには現れませんが、
設計判断のほぼすべてがここから来ています。

**この前提を書いた依頼ファイル（`<対象>_<種別>_<YYMMDD>.md`）や仕様書は
リポジトリに存在しません。** 出どころはクラウドセッションでの会話だけで、
残っているのは初回コミットのメッセージと README の冒頭、そしてこの表です。
「縦置きだったか」を後から確かめたいときは、ここが唯一の記録です。

| 項目 | 決まったこと |
|---|---|
| 設置場所 | 32インチ Android タッチモニター、**縦置き**（1080×1920 想定） |
| 用途 | そこでゲームを遊ばせるのではなく、**メニュー／案内板** |
| 通信 | **オフライン必須**。外部ライブラリ・フォント・API を一切使わない |
| 起動方法 | 単一HTML をブラウザで全画面表示（PWA・APK は見送り） |
| コンテンツ | ゲーム5枚＋動画2本＝**計7枚を同じリングに混在** |
| 動きの指定 | 「クルクル回っていて（スワイプで早く回転可）ゆらゆらしてる」 |
| 操作 | サムネをタップ → 全画面モーダルで説明。動画はその場で再生 |
| 差し替え | **ファイル名固定で置くだけ** |
| 仮素材 | ダミー画像を自動生成して同梱 |
| 配色 | 当初は白黒＋アクセント `#4FADBB` → 後に **白地の明るいトーン** へ変更 |
| 画面上の文字（PC側で決定） | **カードは画像とタイトルだけ**。見出し（GAME LINEUP / TOUCH TO EXPLORE）、操作ヒント（スワイプで回す・タップで詳細）、ジャンル・人数・再生時間の副題、GAME/MOVIE バッジ、再生マークはすべて削除 |

**「クルクル回っていて」を最優先しているため、`snapToFront` は既定で `false`
です。** 最寄りカードで止める挙動は実装済みですが、要望と異なるため無効に
してあります。止めたい場合は `true` にするだけです。

**削った表示物は「消した」のであって「壊した」のではありません。**
`sub:` は任意項目として残っており（書けば出る・省略すれば枠ごと消える）、
詳細モーダル上部の GAME / MOVIE ラベル（`#mtag`）だけは削除の指示が
出ていないので残っています。同じ方針で消すなら次はここです。

---

## 2. コードから読み取れない技術判断

### 2.1 データを外部 JSON にせず HTML に埋め込んでいる理由

`games.json` のような設定ファイルにすると編集は楽ですが、
**`file://` で開いたページからの `fetch` はブラウザが遮断します。**
オフラインでローカルのHTMLを直接開く運用ではデータを読めなくなるため、
`ITEMS` を HTML 内に直接持たせています。

画像と動画は `<img src>` / `<video src>` なので `file://` でも問題なく読めます。
この非対称性が、この構成の理由です。

### 2.2 ダミー画像が SVG である理由

この作業環境に PIL も ImageMagick も無く、JPEG/PNG を生成できませんでした。
SVG なら依存ゼロで、文字や図形も描けます。

拡張子を `jpg → jpeg → png → webp → svg` の順に探す実装にしてあるので、
**同名の `.jpg` を置けばそちらが優先され、ダミーの `.svg` を消す必要はありません。**
「置くだけ」という要件は、この探索順で成立させています。

### 2.3 `color-mix()` を使っていない理由

当初は CSS の `color-mix()` でアクセント色を薄めていましたが、
**Chrome 111 未満では宣言ごと無効になり、背景のグラデーションが消えて
真っ黒（白地版では真っ白）になります。**
設置先の Android WebView のバージョンが不明なため、色の計算は
すべて JavaScript 側（`rgba()` / `lighten()`）に移しました。

### 2.4 アニメーション完了に保険を掛けている理由

`openModal` / `closeModal` は Web Animations API の `onfinish` で状態を戻して
いますが、**画面が裏に回るなどして `onfinish` が発火しないと `animating` が
`true` のまま固まり、モーダルが二度と閉じなくなります。**
無人運用のキオスクでは致命的なので、`once(fn, ms)` でタイムアウトの保険を
入れてあります。

### 2.5 `autoLite`（自動軽量化）を入れた理由

32インチの Android モニターは中身が安価な SoC であることが多く、
設置先の性能を事前に確定できません。実フレームレートを測って
`lite` → `lite2` と段階的に効果を落とす仕組みにしてあります。

---

## 3. 白地テーマにして表面化した不具合

暗い背景では見えなかったものが、白地にしたことで一気に露見しました。
**いずれも白地版で修正済みです。** 再発防止のため原因を残します。

| 不具合 | 原因 | 対処 |
|---|---|---|
| 正面のカードが半透明で、後ろのカードが透けて濁る | 不透明度が最大でも **0.77** にしかならなかった。暗背景では黒に沈んで見えなかった | 正面付近を平坦域として完全不透明に |
| 正面のカードが常にわずかにぼけ、縮んでいる | 7枚だとカードは**最大 25.7 度ずれた位置**で「最も正面」になるため、原寸・ピントが合う瞬間が存在しなかった | `FLAT`（±28度までを正面扱い）を導入。※大きさについては後述のとおり方針変更あり |
| 光る枠が出ない | `.inner` の `overflow:hidden` の外側（`inset:-2px`）に描いていたため切り取られていた | `inset` の `box-shadow` で内側に描画 |
| 光る枠が複数のカードに同時に出る | 連続関数 `pow(n,3)` で出していたため、2枚が同時に正面付近に来ると両方光った | 最も正面に近い1枚だけに限定 |
| 見えないカードがタップを奪う | 不透明度 0 のカードもポインタイベントを受け取っていた | `pointer-events` で遮断 |
| 動画再生中に「見つかりません」が重なる | 拡張子を順に試す仕組みのため、存在しない `.mp4` の `<source>` がエラーを出す。それを「動画なし」と誤判定していた | 全候補が尽きた状態（`networkState === 3`）のときだけ表示 |

**最後の1件は実機では気づきにくい種類の不具合です。**
`videos/video1.mp4` を置いた場合は最初の候補で成功するため発生しません。
`.webm` や `.m4v` で運用したときだけ、再生できているのに案内が重なります。

### カード同士が切り合って見える（PC側で修正）

白地テーマで撮った実機相当のスクリーンショットで、正面のカードの縁が
隣のカードの面に食い込んで見える現象が報告されました。
**z-index や描画順の問題ではなく、カードの面が3D空間で物理的に交差していた**
のが原因です。

- カードは `rotateY(θ) translateZ(R)` で正多角形の各辺に置かれる
- 隣の辺と交わらない最小半径は **`R / W = 0.5 / tan(π / N)`**（W=カード幅）。
  N=7 で 1.038、N=8 で 1.207、N=10 で 1.539
- 旧設定は `--radius: calc(var(--card-w) * 1.00)` で、この下限を割っていた。
  さらに正面カードを 1.08 倍に拡大していたので、実効幅 1.08W → 下限 1.12W
- `preserve-3d` の中では交差した面をブラウザが分割して深度順に描くため、
  JS から書いている `z-index` は効かず、切り合いはそのまま見える

対処は §4.1 の「その後」に書いた `ringK` の自動計算（下限 × `RING_MARGIN` 1.15）と、
正面の拡大をやめる（最大 1.0）ことの2本立てです。
枚数が少ないと下限が小さくなりすぎる（2枚で 0）ので `ringK` には 0.6 の床を
入れてあり、2枚構成でも描画が崩れないことを確認しています。

半径を増やした分、両隣のカードは画面の左右に前より多くはみ出します。
それを抑えるためカード幅を `46vw → 42vw` に下げました。
**この2つ（42vw / 1.15）は依頼者の指示ではなく実装側の判断**なので、
「カードをもっと大きく」と言われたら `--card-w` を戻し、両隣が切れるのを
受け入れる、というトレードオフになります。

### 「正面の平坦域」のその後

`FLAT` は当初、**大きさ・不透明度・ぼかしの3つ**に掛けていました。
その際は正面のカードを 1.08 倍に拡大していましたが、
**拡大すると隣のカードと3Dで交差して互いに切り合って見える**ことが分かり、
その後の修正で大きさだけ扱いを変えています。

- **不透明度・ぼかし** … `FLAT` の平坦域を使う（±28度までは完全に不透明・くっきり）
- **大きさ** … 平坦域を使わず、正面で原寸 1.0、離れるほど連続的に縮小。
  **1.0 を超えて拡大はしない**

交差の根本対策としてリング半径を大きく取る方式（`RING_MARGIN`）も
同時に入っているため、この2つは併せて理解してください。

---

## 4. 実測して決めた数値

すべて Chromium（1080×1920）での計測です。

### 4.1 カード幅とリング半径

1回転ぶん（34サンプル×5点）で「正面カードの画面外へのはみ出し」と
「他のカードが正面カードに被った回数」を測りました。

| カード幅 | 半径倍率 | 最大はみ出し | 被り回数 |
|---|---|---|---|
| 46vw | ×1.00 | **1px** | **3 / 170** |
| 50vw | ×1.00 | 61px | 2 / 170 |
| 54vw | ×1.00 | 118px | 4 / 170 |
| 50vw | ×0.88 | なし | 57 / 170 |
| 52vw | ×0.82 | なし | 12 / 170 |

**この2つは連動していて、カードを大きくすると左右で切れ、半径を小さくすると
カード同士が3Dで交差します。** 7枚・1080px 幅では両立点が狭く、
`46vw × 1.00` が最良でした。

> この値はその後、枚数から半径を自動計算する方式
> （`RING_MARGIN` / `ringK`）に一般化されています。
> 上の表は「なぜ半径を大きく取る必要があるか」の裏付けとして残しています。
>
> **注意: 表の「被り回数 3/170」は、投影した矩形の重なりを数えたもので、
> 3Dでの面の交差は検出していません。** `46vw × 1.00` は表の上では最良でも
> 交差の下限（1.038）を割っており、実際には切り合いが起きていました（§3）。
> 「被りが少ない」＝「交差していない」ではない点に注意してください。
>
> 現在の実効値は `42vw × 1.19`（7枚の場合）です。表の最良値より
> **カードを小さく・半径を大きく**する方向に振られていますが、
> これは表で測っていた「被り」＝3Dの交差を、確率ではなく
> 幾何的にゼロにする方針へ切り替えたためです。
> `ringK = 0.5 / tan(π / 枚数) × RING_MARGIN` は、カードを正多角形の辺に
> 置いたときに隣と交差しない最小半径に、余裕（1.15倍）を掛けたものです。

### 4.2 回転の緩急（`detent`）

等速で回すと、強調表示されているカードが画面中央に居る時間がわずかでした。

| `detent` | 中央±120px に居た割合 |
|---|---|
| 0（等速） | 45% |
| 0.72 | 77% |

※ この計測時点の `autoSpeed` は 0.055 でした。現在は `autoSpeed: 0.032` /
`detent: 0.55` に調整されているため、割合は変わります。
（PC側で「もう少し遅くてよい」との指示で 0.055 → 0.032、一周およそ 6.5秒 → 11秒。
速度を落とすと正面での「溜め」が停止に見えやすいため、`detent` も 0.72 → 0.55 に
合わせて緩めました。この組み合わせは目視で決めており、再計測はしていません。）

### 4.3 描画負荷

ヘッドレスのソフトウェア描画（GPUなし）での相対比較です。
**実機の GPU 描画とは数値が大きく異なります。** 傾向の把握用です。

| 条件 | fps |
|---|---|
| そのまま | 10 |
| カードの `box-shadow` なし | 13 |
| 影とぼかし両方なし | 17 |
| リング自体を非表示 | 45 |

カードの描画（7枚の大きな3Dレイヤー合成）が支配的で、
ぼかし単体の寄与は小さいと分かりました。`autoLite` の段階設計はこれが根拠です。

---

## 5. 検証のやり方（再現手順）

テストスクリプトは一時領域に置いたもので、リポジトリには含まれていません。
やり直す場合の要点を残します。

```js
const { chromium } = require('playwright');
const b = await chromium.launch({
  // Playwright の既定パスとバージョンがずれているため実行ファイルを直接指定
  executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome',
  args: ['--no-sandbox']
});
const p = await b.newPage({ viewport: { width: 1080, height: 1920 } });
await p.goto('file:///.../game-showcase/index.html');
```

### 落とし穴が2つあります

**① ヘッドレスでは `autoLite` が誤作動する。**
ソフトウェア描画だと 10fps 程度しか出ないため、自動軽量化が働いて
ぼかしも影も切れた状態のスクリーンショットになります。
実機相当の見た目を確認したいときは、`autoLite: false` にしたコピーを
別途用意してください。

**② 座標クリックでは狙ったカードを開けない。**
リングは常時回転しており、さらに3Dの重なり順があるため、
カードの中心座標をクリックしても別のカードに当たります。
カード要素へ直接イベントを送ってください。

```js
await p.evaluate(n => {
  const el = document.querySelectorAll('.card')[n].querySelector('.inner');
  const r = el.getBoundingClientRect();
  const o = { bubbles:true, composed:true, pointerId:1, isPrimary:true,
              clientX:r.left+r.width/2, clientY:r.top+r.height/2 };
  el.dispatchEvent(new PointerEvent('pointerdown', o));
  el.dispatchEvent(new PointerEvent('pointerup', o));
}, 5);
```

### PC 側での検証手順（Playwright を使わない場合）

PC（macOS）では Playwright を入れずに、インストール済みの Chrome を
ヘッドレスで直接叩いて確認しました。

```sh
cd game-showcase && python3 -m http.server 8765 &
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --disable-gpu --hide-scrollbars \
  --window-size=1080,1920 --virtual-time-budget=2500 \
  --screenshot=out.png http://localhost:8765/index.html
```

- **Claude in Chrome（ブラウザ拡張）は `file://` を開けません。** ローカル HTTP で
  配信する必要があります。今回は拡張自体が未接続だったので、上記の
  ヘッドレス起動に切り替えました
- **一番重なりやすい姿勢は「2枚が ±25.7° にいる中間」**です。正面で撮っただけでは
  交差の有無は判定できません。`let angle = 0` を `let angle = step / 2` に、
  `autoSpeed` を `0` にした一時コピー（`_mid.html` 等）を作って撮り、撮ったら消します
- 枚数を変えた確認も同様に、`const step = ...` の直前に `ITEMS.length = 2;` を
  差し込んだ一時コピーで行いました

### 実施した検証項目

- モーダルの開閉（ゲーム／動画の両方）
- 自動回転が継続すること
- 画像の拡張子フォールバック（`.jpg` を置くと `.svg` より優先される）
- 枚数を 3 / 7 / 10 に変えたときのリング角度とドットの再配置
- 1回転を通した「はみ出し」と「正面カードの不透明度」
- 動画あり（webm）／なしの両方での表示
- 横向き（1920×1080）でのレイアウト
- JavaScript エラーが出ないこと
- （PC側）正面と中間姿勢（±25.7°）の両方で3D交差が無いこと、2枚構成での描画、
  見出し・ヒント・副題・バッジ削除後に画面下がタイトルだけになること、
  `images/*.png` を置くとダミー `.svg` より優先されること

---

## 6. 環境上の落とし穴

- **画像生成ライブラリが無い。** PIL も ImageMagick も入っていません。
- **Playwright 同梱の ffmpeg は機能が削られている。** `lavfi` 入力が無く、
  テスト用の素材生成には使えません。今回はブラウザの `canvas.toDataURL()` と
  `MediaRecorder` で JPEG と WebM を作って検証しました。
- **GitHub のメールアドレス保護で push が拒否される。**
  `ytakaki75@gmail.com` のままコミットすると
  `push declined due to email privacy restrictions` になります。
  `211468061+ty-gh@users.noreply.github.com` を使ってください。

---

## 6.5 削除で解消した不具合（PC側）

動画カードの中央に出ていた **白い丸は再生ボタン（`.playmark`）の残骸**でした。
本来は丸の中に ▶ を描く設計でしたが、三角を `border-left: 26% solid ...` の
ように **`border-width` にパーセントで指定していたため無効になり**、
丸だけが表示されていました。直す案（`clip-path` か SVG で描き直す）と
消す案を出し、**再生マークと GAME/MOVIE バッジごと削除**する判断になりました。
動画かどうかの区別は、タップ後のモーダルの動作とタイトルで足りる、という判断です。

---

## 6.6 3段リングへの変更（2026-09-19）

「3列にしたい」という要望を確認したところ **上・中・下の3段のリングに分け、
それぞれが回転する** 形が希望だったため、リングを `ROWS = 3` 段に再構成した。
同時に「ワークショップ受付中」のお知らせカード（`type: "info"`、
`images/workshop.svg`）を追加し、カードは計7枚になった。

**7枚を3段に分けると 3/2/2 枚になり、そのままでは成立しない。**
`FADE = 112`（正面から112°より奥は完全に消す）のため、リング上で見える枚数は
おおよそ `2×112 / (360/枚数) + 1` 枚。2枚のリングでは裏側の1枚が常に 180° に
いて見えず、「1枚が回っているだけ」になる。

そこで **1段あたりの席数 `SLOTS` を全段で共通にし、足りない段は同じカードを
繰り返して埋める**方式にした（`CONFIG.minSlots`、既定 6）。
2枚の段では同じカードが3回並ぶので、**正面の左右に同じカードが同時に見える**。
これは7枚を3段に分ける以上避けられない（席数を減らすと重複が減るのではなく
リングが空くだけ）。9枚以上になれば自然に解消する。
席数が共通なので `step` / `ringK` / `FLAT` / 緩急（`detent`）/ 矢印キーが
1段のときと同じ式のまま使える。段ごとに別の席数にする案は、リングの大きさと
回転の刻みが段で変わって不揃いに見えるため見送った。

そのほかの判断：

- 各段は `.band` にまとめ、**perspective は段ごと**に持たせた。舞台全体で
  1つにすると上段・下段が消失点から外れて別の角度に見えるため
- 正面カードのタイトルは段ごとにその段の直下へ。ドットもその段の
  カード数（重複を除く）ぶんだけ出す
- 指で押さえている段だけ止め、他の段は回り続ける。段の判定は
  `pointerdown` の要素（隙間なら Y 座標が最も近い段）
- 段ごとに初期位相を `step/ROWS` ずつずらし、3段が同時に切り替わらないようにした
- カードが3倍になるため、`opacity 0` のカードは `visibility:hidden` にして
  描画対象から外す（`pointer-events` と同じキャッシュ方式）。実機での
  フレームレートは未検証（8章）
- `#mtag` は `tag:` で上書き可能にし、`info` は既定で `INFO`

---

## 7. 検討したが採用しなかったこと

| 案 | 見送った理由 |
|---|---|
| `games.json` に設定を外出し | `file://` では読めない（2.1参照） |
| PWA 化 / APK 化 | 中身は同じHTMLなので後から移行可能。まず単一HTMLで完成させる方針に |
| 動画を別枠・二重リングに分ける | 1つのリングに混ぜるほうが操作が単純で、追加・削除も行単位で済む |
| 観覧車型（縦回転）／平面の円周回転 | 縦置き32インチでは上部のカードに手が届かない |
| カードごとに違うテーマカラー | 当初は7色。白黒＋アクセント1色へ変更 |
| サムネのファイル名を `movie1.jpeg` に変更 | 検討したが `video1` のまま据え置き |
| 再生マークの三角を描き直す（`clip-path` / SVG） | マーク自体を消す方針になったため不要に（6.5参照） |
| `--ring-k` を CSS で手動指定できるようにする | JS が起動時に必ず上書きする方式のまま。CSS 側の値はフォールバック扱いで、コメントにその旨を明記 |
| 3段それぞれに全カードを並べる（段は位相違い） | どの段にも同じ7枚が出て冗長。「段に分ける」という要望とも合わない（6.6参照） |
| 3段を別々の席数にする | 段ごとにリングの大きさと回転の刻みが変わり不揃いに見える（6.6参照） |

---

## 8. 未検証・積み残し

**実機で最初に確認すべきこと**

- Android 実機での動作全般（フレームレート、タッチ追従、全画面表示）
- `autoLite` が実機でどの段階まで落ちるか。落ちすぎるなら閾値の調整が必要
- **`.mp4` での再生は未検証です。** 検証環境で mp4 を生成できなかったため、
  `.webm` でのみ確認しています。形式判定のロジックは共通です
- 3段化でカードが 18 枚（6席×3段）になった後の実機フレームレート。
  落ちるなら `minSlots` を下げる（4 で 12 枚）か `maxBlur: 0` が第一候補
- 音声つき動画の自動再生可否（実機のポリシー次第。拒否された場合は
  自動的に消音で再生するフォールバックを入れてあります）
- 長時間の連続稼働（メモリ、焼き付き）

**リポジトリの状態（2026-09-18 時点）**

- `images/game1〜5.png`, `video1.png`, `video2.png` は動作確認用に同じ画像
  （400×397 の正方形）を7つ複製したもので、**コミットしていません**。
  正方形なので 16:9 のカードでは上下が切れて表示されます。実素材は横長 16:9 を
- 作業ブランチ `claude/game-thumbnail-animation-1l8gnx` は push 済みですが
  **main には未マージ・PR 未作成**です

**見た目について**

現在のダミー画像は均質なライトグレーなので、実際のサムネを入れると
印象がかなり変わります（テスト画像を入れた時点で、白い下地に対して
カードがかなり強く出ることは確認済み）。下地 `--bg` の明るさ、影の強さ、アクセントの
効かせ具合は、実素材を入れてから再調整する前提でいてください。

**改善候補**

- 動画モーダルを開いた直後、読み込み完了まで黒画面になります。
  サムネ画像を `<video poster>` に指定すれば、カードからモーダルへの
  拡大アニメーションが途切れず繋がります（未実装）。
- 見出しを消した分、リングは画面の縦中央寄りに下がり、上部が空いています。
  実機で「上が寂しい」となったら `#stage` の上下バランスか `--card-w` で調整
